import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    images: {
      domains: ['placehold.co','s3.amazonaws.com','bucket-eternal.s3.us-east-1.amazonaws.com'],
    },
};

export default nextConfig;
