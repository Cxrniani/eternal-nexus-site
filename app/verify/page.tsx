// /app/verify/page.tsx pagina para verificação de usuario, ainda é necessario fazer com que o email não seja um campo editavel,
//  apareça como H1 e um botão de reenviar email
import Verify from "@/components/Verify";

export default function VerifyPage() {
  return <Verify />;
}
